---
plugin: aws_ec2

aws_access_key: access key
aws_secret_key: scret key

regions:
  - us-west-2

keyed_groups:
  - key: tags
    prefix: tag
  - prefix: instance_type
    key: instance_type
